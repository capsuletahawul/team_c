// src/routes/contactRoutes.ts
//
// Maps URL + method -> controller function. Nothing else lives here.
//
// Architecture scaffold: router is created and exported so server.ts has
// something real to mount. The actual endpoint definition belongs to
// whoever builds contactController.ts — add it here.

import { Router } from "express";

const router = Router();

// router.post("/", contactController.submitContact);

export default router;